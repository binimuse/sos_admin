// ignore_for_file: constant_identifier_names

enum Gender {
  female,
  male,
}

enum FaceDetectionResultErrorType {
  POOR_QUALITY,
}


enum AppTokenRefresherState{
  REFRESHING,
  TOKEN_INVALID,
  TOKEN_VALID,
  NETWORK_ERROR,
  OTHER,
}

enum Roles{
ossc,
contract_immigration,
rejecter
}